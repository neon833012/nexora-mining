{
  "name": "nexora-mining",
  "version": "1.0.0",
  "private": true,
  "description": "Nexora Enterprise Web3 Cloud Mining Platform",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "node build.js",
    "preview": "npm --prefix frontend run preview"
  }
}
